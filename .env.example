# Tracking — master toggle
NEXT_PUBLIC_ENABLE_TRACKING="false"

# Google Tag Manager (contains Meta Pixel web-side)
NEXT_PUBLIC_GTM_ID=""

# Meta Pixel — client-side (for Advanced Matching re-init)
NEXT_PUBLIC_META_PIXEL_ID=""

# Meta CAPI — server-side (secrets, never exposed to browser)
META_PIXEL_ID=""
META_CAPI_ACCESS_TOKEN=""

# Meta test events (optional — for Events Manager validation)
NEXT_PUBLIC_META_TEST_EVENT_CODE=""
META_TEST_EVENT_CODE=""

# n8n webhook (optional — for Lead event forwarding)
N8N_WEBHOOK_URL=""
